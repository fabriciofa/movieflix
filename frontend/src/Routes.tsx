import { BrowserRouter } from "react-router-dom";
import Navbar from "./components/Navbar";

const Routes = () => (
    <BrowserRouter>
        <Navbar />
    </BrowserRouter>
);

export default Routes;